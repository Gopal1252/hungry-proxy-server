{
  "name": "hungry-proxy-server",
  "version": "1.0.0",
  "description": "cors proxy server for hungry react app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gopal Gupta",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "cross-fetch": "^4.0.0",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}
